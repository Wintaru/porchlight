// The seeded content-note tag (SPEC.md §7). The editor's checkbox adds or removes it
// and hides it from the chips; the post page blurs a cover when a post carries it. The
// slug must match the `mature` row supabase/seed.sql inserts into `public.tags`.
export const MATURE_TAG = "mature";
